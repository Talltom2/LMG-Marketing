import {createHash,timingSafeEqual} from "crypto";import {NextResponse} from "next/server";
const hash=(v:string)=>createHash("sha256").update(v).digest("hex");
export async function POST(req:Request){const{password}=await req.json();const configured=process.env.LMG_APP_PASSWORD,secret=process.env.LMG_AUTH_SECRET;if(!configured||!secret)return NextResponse.json({error:"Authentication is not configured."},{status:503});const a=Buffer.from(hash(String(password??""))),b=Buffer.from(hash(configured));if(a.length!==b.length||!timingSafeEqual(a,b))return NextResponse.json({error:"Incorrect password."},{status:401});const res=NextResponse.json({ok:true});res.cookies.set("lmg_auth",hash(`${configured}:${secret}`),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"strict",path:"/",maxAge:60*60*12});return res;}