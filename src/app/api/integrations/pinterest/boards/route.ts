import {NextResponse} from "next/server";import {createPinterestBoard,deletePinterestBoard,getPinterestBoardManager,updatePinterestBoard} from "@/lib/integrations/pinterest/board-manager";export const dynamic="force-dynamic";
export async function GET(){try{return NextResponse.json(await getPinterestBoardManager())}catch(e){return NextResponse.json({message:e instanceof Error?e.message:"Unable to load Pinterest boards."},{status:502})}}
export async function POST(req:Request){try{const b=await req.json();if(!String(b.name??"").trim())return NextResponse.json({message:"Board name is required."},{status:400});return NextResponse.json(await createPinterestBoard(String(b.name).trim(),String(b.description??"")))}catch(e){return NextResponse.json({message:e instanceof Error?e.message:"Unable to create board."},{status:502})}}
export async function PATCH(req:Request){try{const b=await req.json();if(!b.id||!String(b.name??"").trim())return NextResponse.json({message:"Board id and name are required."},{status:400});return NextResponse.json(await updatePinterestBoard(String(b.id),String(b.name).trim(),String(b.description??"")))}catch(e){return NextResponse.json({message:e instanceof Error?e.message:"Unable to update board."},{status:502})}}
export async function DELETE(req:Request){try{const b=await req.json();if(!b.id)return NextResponse.json({message:"Board id is required."},{status:400});await deletePinterestBoard(String(b.id));return NextResponse.json({deleted:true})}catch(e){return NextResponse.json({message:e instanceof Error?e.message:"Unable to delete board."},{status:502})}}
