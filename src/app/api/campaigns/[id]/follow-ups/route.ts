import {NextResponse} from "next/server";import {db} from "@/lib/db";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;return NextResponse.json({actions:await db.campaignFollowUpAction.findMany({where:{campaignId:id},orderBy:{createdAt:"desc"}})})}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,b=await req.json(),title=String(b.title??"").trim(),description=String(b.description??"").trim();if(!title||!description)return NextResponse.json({error:"Title and description are required."},{status:400});const action=await db.campaignFollowUpAction.create({data:{campaignId:id,title,description,source:"CHATGPT",status:"PROPOSED",dueAt:b.dueAt?new Date(String(b.dueAt)):null}});return NextResponse.json({action},{status:201})}
