"use client";
import Link from "next/link";import {useEffect,useMemo,useState} from "react";
type P={sku:string;name:string;productType:string|null;health:"GREEN"|"YELLOW"|"RED";buyBoxWinRate:number|null;traffic:string|null;priceCompetitive:boolean|null;reasons:string[]};
const color=(h:P["health"])=>h==="RED"?"#dc2626":h==="YELLOW"?"#eab308":"#16a34a";
export default function WalmartProductHealthList(){const[products,setProducts]=useState<P[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[q,setQ]=useState("");useEffect(()=>{fetch("/api/integrations/walmart/products",{cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.message);setProducts(b.products??[])}).catch(e=>setError(e instanceof Error?e.message:"Unable to load products.")).finally(()=>setLoading(false));},[]);const filtered=useMemo(()=>{const n=q.trim().toLowerCase();return n?products.filter(p=>`${p.sku} ${p.name} ${p.productType??""}`.toLowerCase().includes(n)):products;},[products,q]);return <section className="panel"><div><p className="eyebrow">Active Product Health</p><h2>Walmart products</h2><p>Click any active SKU to open its full diagnostic workbench.</p></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search SKU or product" style={{padding:10,border:"1px solid #aaa",borderRadius:8,minWidth:280,marginBottom:14}}/>{loading&&<p>Loading Walmart product health…</p>}{error&&<p>{error}</p>}<div style={{display:"grid",gap:8,maxHeight:620,overflow:"auto"}}>{filtered.map(p=><Link key={p.sku} href={`/diagnostics/product?sku=${encodeURIComponent(p.sku)}`} target="_blank" style={{textDecoration:"none",color:"inherit"}}><article style={{display:"grid",gridTemplateColumns:"22px minmax(240px,2fr) minmax(110px,.7fr) minmax(110px,.7fr) minmax(140px,1fr)",gap:10,alignItems:"center",padding:"10px 12px",border:"1px solid #ddd",borderRadius:8}}><span title={p.health} style={{width:16,height:16,borderRadius:"50%",background:color(p.health),display:"inline-block"}}/><div><strong>{p.name}</strong><br/><small>{p.sku}{p.productType?` · ${p.productType}`:""}</small></div><div>Traffic<br/><strong>{p.traffic??"—"}</strong></div><div>Buy Box<br/><strong>{p.buyBoxWinRate==null?"—":`${p.buyBoxWinRate.toFixed(1)}%`}</strong></div><div><strong>{p.health}</strong><br/><small>{p.reasons.join(" · ")||"No live issue detected"}</small></div></article></Link>)}</div>{!loading&&<p><small>{filtered.length} active products shown.</small></p>}</section>}
