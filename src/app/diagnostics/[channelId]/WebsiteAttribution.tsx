"use client";
import {useEffect,useState} from "react";
import AutoFixButton from "./AutoFixButton";
type Row={source:string;label:string;health:"GREEN"|"YELLOW"|"RED";stage:string;diagnosis:string;recommendation:string;sessions:number;productViews:number;addToCarts:number;checkoutStarts:number;purchases:number;revenue:number;conversion:number|null;revenuePerSession:number|null};
const color=(h:Row["health"])=>h==="RED"?"#dc2626":h==="YELLOW"?"#eab308":"#16a34a";const pct=(v:number|null)=>v==null?"—":`${(v*100).toFixed(1)}%`;const money=(v:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(v);
export default function WebsiteAttribution(){const[rows,setRows]=useState<Row[]>([]);const[channelId,setChannelId]=useState("");const[channelName,setChannelName]=useState("Laughing Moose Gifts Website");const[error,setError]=useState("");useEffect(()=>{fetch("/api/integrations/lmg-analytics/attribution",{cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.message);setRows(b.sources??[]);setChannelId(b.channelId??"");setChannelName(b.channelName??"Laughing Moose Gifts Website")}).catch(e=>setError(e instanceof Error?e.message:"Unable to load attribution."));},[]);return <section className="panel" style={{display:"block"}}><div><p className="eyebrow">TRAFFIC SOURCE INTELLIGENCE</p><h2>Which channels send traffic that buys?</h2><p>Last 7 days · source-by-source funnel diagnosis, recommended response, and approved corrective action.</p></div>{error&&<p>{error}</p>}<div style={{display:"grid",gap:10,marginTop:16}}>{rows.map(r=><article key={r.source} style={{display:"grid",gridTemplateColumns:"18px minmax(125px,1.1fr) repeat(5,minmax(68px,.55fr)) minmax(280px,2.4fr)",gap:10,alignItems:"start",padding:"12px",border:"1px solid #ddd",borderRadius:8}}><span title={r.health} style={{width:15,height:15,borderRadius:"50%",background:color(r.health),marginTop:4}}/><strong>{r.label}</strong><span>Sessions<br/><strong>{r.sessions}</strong></span><span>Views<br/><strong>{r.productViews}</strong></span><span>Carts<br/><strong>{r.addToCarts}</strong></span><span>Orders<br/><strong>{r.purchases}</strong></span><span>Conv.<br/><strong>{pct(r.conversion)}</strong></span><div><strong>{money(r.revenue)} · {r.stage.replaceAll("_"," ")}</strong><p style={{margin:"6px 0 0"}}><small>{r.diagnosis}</small></p><p style={{margin:"6px 0 0"}}><small><strong>Recommended:</strong> {r.recommendation}</small></p>{r.health!=="GREEN"&&r.sessions>0&&channelId&&<AutoFixButton channelId={channelId} channelName={`${channelName} · ${r.label}`} layer={r.stage} title={`${r.label}: ${r.stage.replaceAll("_"," ")} issue`} observation={`${r.sessions} sessions, ${r.productViews} product views, ${r.addToCarts} add-to-carts, ${r.checkoutStarts} checkout starts, ${r.purchases} purchases, ${pct(r.conversion)} conversion over the last 7 days. ${r.diagnosis}`} likelyCause={`The weakest measured funnel stage for ${r.label} is ${r.stage.replaceAll("_"," ").toLowerCase()}.`} recommendation={r.recommendation} confidence={r.sessions>=20?.9:.72}/>}</div></article>)}</div></section>}
