"use client";
import {useEffect,useState} from "react";
type Check={key:string;label:string;health:"GREEN"|"YELLOW"|"RED";value:string;detail:string};
type Finding={layer:string;severity:string;title:string;observation:string;recommendation:string;confidence:number};
const color=(h:string)=>h==="RED"?"#dc2626":h==="YELLOW"?"#eab308":"#16a34a";
export default function WebsiteConfigHealth(){const[data,setData]=useState<{health:string;checks:Check[];findings:Finding[];dataGaps:string[]}|null>(null);const[error,setError]=useState("");useEffect(()=>{fetch("/api/integrations/woocommerce/config-health",{cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.message);setData(b)}).catch(e=>setError(e instanceof Error?e.message:"Unable to inspect website configuration."));},[]);return <section className="panel" style={{display:"block"}}><div><p className="eyebrow">WEBSITE ADMIN / CONFIGURATION HEALTH</p><h2>WooCommerce settings and checkout readiness</h2><p>Checks store settings, inventory controls, taxes, checkout, payment gateways, shipping, HTTPS/system status, and API readability.</p></div>{error&&<p>{error}</p>}{data&&<><div style={{display:"flex",alignItems:"center",gap:10,margin:"12px 0 16px"}}><span style={{width:20,height:20,borderRadius:"50%",background:color(data.health)}}/><strong>{data.health}</strong></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:10}}>{data.checks.map(c=><article className="module" key={c.key}><div style={{display:"flex",alignItems:"center",gap:8}}><span style={{width:13,height:13,borderRadius:"50%",background:color(c.health)}}/><strong>{c.label}</strong></div><p><strong>{c.value}</strong></p><small>{c.detail}</small></article>)}</div>{data.findings.length>0&&<div style={{marginTop:16}}><h3>Configuration findings</h3>{data.findings.map((f,i)=><article className="module" key={`${f.layer}-${i}`} style={{marginTop:8}}><p className="eyebrow">{f.layer.replaceAll("_"," ")} · {f.severity}</p><h4>{f.title}</h4><p>{f.observation}</p><p><strong>Recommended:</strong> {f.recommendation}</p></article>)}</div>}</>}</section>}
