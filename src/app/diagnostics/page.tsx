import Link from "next/link";
import { runDiagnosticEngine, type DiagnosticSeverity } from "@/lib/intelligence/diagnostics";
import { getWalmartDiagnosticSnapshot } from "@/lib/integrations/walmart/diagnostics";
import { evaluateWalmartSnapshot } from "@/lib/integrations/walmart/evaluate";
import { getWooDiagnosticSnapshot } from "@/lib/integrations/woocommerce/diagnostics";
import { evaluateWooSnapshot } from "@/lib/integrations/woocommerce/evaluate";
import { getPinterestConfigHealth } from "@/lib/integrations/pinterest/config-health";
import { getTikTokConfigHealth } from "@/lib/integrations/tiktok/config-health";

export const dynamic = "force-dynamic";
const severityLabel: Record<DiagnosticSeverity,string>={CRITICAL:"Critical",WARNING:"Warning",WATCH:"Watch",HEALTHY:"Healthy",DATA_GAP:"Data gap"};
const pct=(v:number|null)=>v==null?"—":`${v>=0?"+":""}${(v*100).toFixed(0)}%`;
const money=(v:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(v);
function trafficState(s:DiagnosticSeverity){if(s==="CRITICAL")return"RED";if(s==="HEALTHY")return"GREEN";return"YELLOW";}
function TrafficLight({severity}:{severity:DiagnosticSeverity}){const state=trafficState(severity);const dot=(c:"RED"|"YELLOW"|"GREEN",b:string)=><span aria-hidden="true" style={{width:18,height:18,borderRadius:"50%",display:"inline-block",background:state===c?b:"#d1d5db",border:"1px solid rgba(0,0,0,.12)"}}/>;return <div style={{display:"flex",alignItems:"center",gap:7,marginBottom:10}}>{dot("RED","#dc2626")}{dot("YELLOW","#eab308")}{dot("GREEN","#16a34a")}<strong style={{marginLeft:4}}>{state}</strong></div>}
export default async function DiagnosticsPage(){const report=await runDiagnosticEngine(7);let walmartNative:any=null;let wooNative:any=null;let pinterestNative:any=null;let tiktokNative:any=null;try{const snapshot=await getWalmartDiagnosticSnapshot();walmartNative={snapshot,evaluation:evaluateWalmartSnapshot(snapshot)}}catch{}try{const snapshot=await getWooDiagnosticSnapshot();wooNative={snapshot,evaluation:evaluateWooSnapshot(snapshot)}}catch{}try{pinterestNative=await getPinterestConfigHealth()}catch{}try{tiktokNative=await getTikTokConfigHealth()}catch{}const actionable=report.findings.filter(f=>f.severity!=="HEALTHY"&&!/pinterest|tiktok/i.test(f.channelName));const pinterestSeverity=(pinterestNative?.health==="RED"?"CRITICAL":pinterestNative?.health==="GREEN"?"HEALTHY":"WARNING") as DiagnosticSeverity;const tiktokSeverity=(tiktokNative?.health==="RED"?"CRITICAL":tiktokNative?.health==="GREEN"?"HEALTHY":"WARNING") as DiagnosticSeverity;return <main><header><p className="eyebrow">Laughing Moose Gifts</p><h1>Diagnostic Center</h1><p className="subtitle">Expected → Actual → Anomaly → Cause → Recommendation → Corrective Action → Learning</p><p><Link href="/">← Marketing Intelligence</Link></p></header><section className="panel"><div><p className="eyebrow">Diagnostic Engine</p><h2>What needs attention?</h2></div><div className="empty-state"><strong>{actionable.length+(pinterestNative?.findings?.length??0)+(tiktokNative?.findings?.length??0)}+ diagnostic signals detected</strong><p>Click a platform card for detailed diagnostics. Green = healthy, Yellow = watch/warning/data gap, Red = critical.</p></div></section><section className="modules"><Link href="/diagnostics/pinterest" target="_blank" style={{color:"inherit",textDecoration:"none"}}><article className="module"><TrafficLight severity={pinterestSeverity}/><p className="eyebrow">{severityLabel[pinterestSeverity]}</p><h3>Pinterest</h3><p>Configuration · catalog · boards · products · traffic</p><strong>Open diagnostics ↗</strong></article></Link><Link href="/diagnostics/tiktok" target="_blank" style={{color:"inherit",textDecoration:"none"}}><article className="module"><TrafficLight severity={tiktokSeverity}/><p className="eyebrow">{severityLabel[tiktokSeverity]}</p><h3>TikTok</h3><p><strong>Configuration health</strong><br/>{tiktokNative?`${tiktokNative.checks.length} settings checked · ${tiktokNative.findings.length} findings`:"Configuration telemetry unavailable"}</p><p>Catalog · Pixel · Events API · Ads · TikTok Shop</p><strong>Open diagnostics ↗</strong></article></Link>{report.channels.filter(channel=>!/pinterest|tiktok/i.test(channel.channelName)).map(channel=>{const isWalmart=/walmart/i.test(channel.channelName);const isWebsite=/website|woocommerce|laughing moose gifts site/i.test(channel.channelName);const nativeSeverity=(isWalmart&&walmartNative?.evaluation?.health?walmartNative.evaluation.health:isWebsite&&wooNative?.evaluation?.health?wooNative.evaluation.health:channel.health) as DiagnosticSeverity;return <Link href={`/diagnostics/${encodeURIComponent(channel.channelId)}`} target="_blank" key={channel.channelId} style={{color:"inherit",textDecoration:"none"}}><article className="module"><TrafficLight severity={nativeSeverity}/><p className="eyebrow">{severityLabel[nativeSeverity]}</p><h3>{channel.channelName}</h3><p><strong>{money(channel.currentRevenue)}</strong><br/>{pct(channel.revenueDeltaPct)} vs prior 7 days</p><strong>Open diagnostics ↗</strong></article></Link>})}</section></main>}
