"use client";
import Link from "next/link";import {FormEvent,useState} from "react";
type Result={sku:string;name:string;health:"GREEN"|"YELLOW"|"RED";summary:any;findings:Array<any>;nextEvidence:string;salesTarget?:{minUnitsPerDay:number;maxUnitsPerDay:number;trafficByConversion:Array<{conversionRatePct:number;minDailyVisits:number;maxDailyVisits:number}>}};
export default function ProductDiagnostics(){const[sku,setSku]=useState("");const[result,setResult]=useState<Result|null>(null);const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");setResult(null);try{const r=await fetch(`/api/integrations/walmart/product-diagnostics?sku=${encodeURIComponent(sku)}`,{cache:"no-store"});const b=await r.json();if(!r.ok)throw new Error(b.message);setResult(b)}catch(e){setError(e instanceof Error?e.message:"Unable to diagnose product.")}finally{setLoading(false)}}const s=result?.summary;return <main><header><p className="eyebrow">Walmart Intelligence</p><h1>Product Diagnostic Workbench</h1><p className="subtitle">SKU → Product Type → inventory → offer → Buy Box → visibility → diagnosis → corrective action</p><p><Link href="/diagnostics">← Diagnostic Center</Link></p></header><section className="panel"><div><h2>Diagnose a Walmart SKU</h2><p>Enter the LMG, Sellerchamp or Walmart seller SKU. The workbench will resolve common SKU aliases automatically.</p></div><form onSubmit={submit} style={{display:"flex",gap:10,flexWrap:"wrap"}}><input value={sku} onChange={e=>setSku(e.target.value)} placeholder="Enter SKU" required style={{padding:12,minWidth:280,border:"1px solid #aaa",borderRadius:8}}/><button disabled={loading} style={{padding:"12px 16px",borderRadius:8,fontWeight:700}}>{loading?"Diagnosing…":"Diagnose Product"}</button></form>{error&&<p>{error}</p>}</section>{result&&<><section className="panel"><div><p className="eyebrow">PRODUCT HEALTH · {result.health}</p><h2>{result.name}</h2><p>SKU {result.sku}</p></div><div className="modules"><article className="module"><h3>Catalog & inventory</h3><p>Status: <strong>{s.publishStatus}</strong><br/>Product Type: <strong>{s.productType??"—"}</strong><br/>{s.expectedProductType&&<>Expected from item identity: <strong>{s.expectedProductType}</strong><br/></>}Inventory: <strong>{s.inventory??"—"}</strong><br/>Fulfillment: <strong>{s.fulfillment??"—"}</strong></p></article><article className="module"><h3>Offer</h3><p>Buy Box win: <strong>{s.buyBoxWinRate==null?"—":`${s.buyBoxWinRate.toFixed(1)}%`}</strong><br/>Price competitive: <strong>{s.priceCompetitive==null?"—":s.priceCompetitive?"Yes":"No"}</strong><br/>Current price: <strong>{s.currentPrice==null?"—":`$${s.currentPrice.toFixed(2)}`}</strong><br/>Suggested price: <strong>{s.suggestedPrice==null?"—":`$${s.suggestedPrice.toFixed(2)}`}</strong></p></article><article className="module"><h3>Visibility & demand</h3><p>Walmart traffic band: <strong>{s.traffic??"—"}</strong><br/>30-day GMV: <strong>{s.gmv30==null?"—":`$${s.gmv30.toFixed(2)}`}</strong></p><small>Pricing Insights traffic is a qualitative band, not a visit count.</small></article></div></section>{result.salesTarget&&<section className="panel"><div><p className="eyebrow">SALES TARGET MATH</p><h2>Traffic needed for {result.salesTarget.minUnitsPerDay}–{result.salesTarget.maxUnitsPerDay} units/day</h2><p>Required product visits depend on conversion. Item Performance will let us replace these scenarios with this SKU's actual conversion rate.</p></div><div className="modules">{result.salesTarget.trafficByConversion.map(row=><article className="module" key={row.conversionRatePct}><h3>{row.conversionRatePct}% conversion</h3><p><strong>{row.minDailyVisits}–{row.maxDailyVisits}</strong><br/>product visits per day</p></article>)}</div></section>}<section className="panel"><div><p className="eyebrow">ROOT-CAUSE ANALYSIS</p><h2>{result.findings.length?`${result.findings.length} issue${result.findings.length===1?"":"s"} detected`:"No immediate live-data issue detected"}</h2></div><div className="modules">{result.findings.map((f,i)=><article className="module" key={`${f.layer}-${i}`}><p className="eyebrow">{f.layer.replaceAll("_"," ")} · {f.severity}</p><h3>{f.title}</h3><p><strong>Observation:</strong> {f.observation}</p><p><strong>Likely cause:</strong> {f.likelyCause}</p><p><strong>Confidence:</strong> {Math.round(f.confidence*100)}%</p><p><strong>Recommended action:</strong> {f.recommendation}</p></article>)}</div><p><strong>Next evidence:</strong> {result.nextEvidence}</p></section></>}</main>}
