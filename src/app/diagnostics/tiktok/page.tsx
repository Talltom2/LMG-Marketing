import Link from "next/link";
import {getTikTokConfigHealth} from "@/lib/integrations/tiktok/config-health";
import AutoFixButton from "../[channelId]/AutoFixButton";
export const dynamic="force-dynamic";
const color=(h:string)=>h==="RED"?"#dc2626":h==="YELLOW"?"#eab308":"#16a34a";
const findingHealth=(s:string)=>s==="CRITICAL"?"RED":s==="WARNING"||s==="WATCH"?"YELLOW":"GREEN";
export default async function TikTokDiagnostics(){const data=await getTikTokConfigHealth();return <main><header><p className="eyebrow">Laughing Moose Gifts</p><h1>TikTok Diagnostics</h1><p className="subtitle">Configuration → catalog → Pixel/Events → content → traffic → conversion → TikTok Shop → corrective action</p><p><Link href="/diagnostics">← Diagnostic Center</Link> · <Link href="/diagnostics/tiktok-products">TikTok Product Summary →</Link> · <Link href="/diagnostics/tiktok-product">Product Workbench →</Link></p></header><section className="panel"><div><p className="eyebrow">PLATFORM HEALTH</p><h2>TikTok Configuration</h2><div style={{display:"flex",gap:10,alignItems:"center"}}><span style={{width:22,height:22,borderRadius:"50%",background:color(data.health)}}/><strong>{data.health}</strong></div></div><p>Checks API authorization, Ads account, Pixel, Events API, catalog connectivity and TikTok Shop readiness.</p></section><section className="panel" style={{display:"block"}}><p className="eyebrow">CONFIGURATION SETTINGS DIAGNOSIS</p><h2>Current TikTok configuration</h2><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:10}}>{data.checks.map(c=><article className="module" key={c.key}><div style={{display:"flex",gap:8,alignItems:"center"}}><span style={{width:14,height:14,borderRadius:"50%",background:color(c.health)}}/><strong>{c.label}</strong></div><p><strong>{c.value}</strong></p><small>{c.detail}</small></article>)}</div></section><section className="panel" style={{display:"block"}}><p className="eyebrow">CONFIGURATION FINDINGS</p><h2>{data.findings.length?`${data.findings.length} issues identified`:"No configuration problems detected"}</h2><div style={{display:"grid",gap:10}}>{data.findings.map((f,i)=>{const h=findingHealth(f.severity);return <article className="module" key={`${f.layer}-${i}`}><div style={{display:"flex",gap:9,alignItems:"center"}}><span style={{width:16,height:16,borderRadius:"50%",background:color(h)}}/><p className="eyebrow" style={{margin:0}}>{f.layer.replaceAll("_"," ")} · {f.severity}</p></div><h3>{f.title}</h3><p><strong>Observation:</strong> {f.observation}</p><p><strong>Likely cause:</strong> {f.likelyCause}</p><p><strong>Recommended:</strong> {f.recommendation}</p><p><strong>Confidence:</strong> {Math.round(f.confidence*100)}%</p><AutoFixButton channelId={data.channelId} channelName={data.channelName} layer={f.layer} title={f.title} observation={f.observation} likelyCause={f.likelyCause} recommendation={f.recommendation} confidence={f.confidence}/></article>})}</div></section></main>}
