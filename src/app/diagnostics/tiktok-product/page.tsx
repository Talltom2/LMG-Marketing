import Link from "next/link";
import {diagnoseTikTokProduct} from "@/lib/integrations/tiktok/product-health";
import AutoFixButton from "../[channelId]/AutoFixButton";
export const dynamic="force-dynamic";
const color=(h:string)=>h==="RED"?"#dc2626":h==="YELLOW"?"#eab308":"#16a34a";
const pct=(v:number|null|undefined)=>v==null?"—":`${(v*100).toFixed(2)}%`;
const money=(v:number|null|undefined)=>v==null?"—":`$${Number(v).toFixed(2)}`;
type Props={searchParams:Promise<{sku?:string}>};
export default async function TikTokProductWorkbench({searchParams}:Props){const {sku=""}=await searchParams;let result:any=null;let error="";if(sku){try{result=await diagnoseTikTokProduct(sku)}catch(e){error=e instanceof Error?e.message:"Unable to diagnose TikTok product."}}return <main><header><p className="eyebrow">Laughing Moose Gifts · TikTok</p><h1>TikTok Product Diagnostic Workbench</h1><p className="subtitle">Catalog → sync/eligibility → creative → exposure → engagement → website conversion → Shop → corrective action</p><p><Link href="/diagnostics/tiktok-products">← TikTok Product Summary</Link> · <Link href="/diagnostics/tiktok">TikTok Diagnostics</Link></p></header><section className="panel"><div><h2>Diagnose a TikTok/WooCommerce SKU</h2><p>Until the TikTok product API is authorized, WooCommerce identifies the product and TikTok-attributed website telemetry supplies performance evidence.</p></div><form method="GET" action="/diagnostics/tiktok-product" style={{display:"flex",gap:10,flexWrap:"wrap"}}><input name="sku" defaultValue={sku} placeholder="Enter SKU" required style={{padding:12,minWidth:280,border:"1px solid #aaa",borderRadius:8}}/><button style={{padding:"12px 16px",borderRadius:8,fontWeight:700}}>Diagnose Product</button></form>{error&&<p>{error}</p>}</section>{result&&<><section className="panel"><div style={{width:"100%"}}><div style={{display:"flex",alignItems:"center",gap:10}}><span style={{width:22,height:22,borderRadius:"50%",background:color(result.health)}}/><p className="eyebrow" style={{margin:0}}>PRODUCT HEALTH</p></div><h2>{result.name}</h2><p>SKU {result.sku}</p></div><div className="modules"><article className="module"><h3>WooCommerce source</h3><p>Status: <strong>{result.summary.status}</strong><br/>Visibility: <strong>{result.summary.visibility}</strong><br/>Stock: <strong>{result.summary.stock??"—"}</strong><br/>Price: <strong>{money(result.summary.currentPrice)}</strong></p></article><article className="module"><h3>TikTok readiness</h3><p>Catalog: <strong>{result.tiktok.catalogConnected?"Connected":"Not connected"}</strong><br/>Pixel: <strong>{result.tiktok.pixelConnected?"Connected":"Not connected"}</strong><br/>Events API: <strong>{result.tiktok.eventsConnected?"Connected":"Not connected"}</strong><br/>TikTok Shop: <strong>{result.tiktok.shopConnected?"Connected":"Not connected"}</strong></p></article><article className="module"><h3>TikTok-attributed funnel</h3>{result.tiktok.sourcePerformance?<p>Views: <strong>{result.tiktok.sourcePerformance.productViews}</strong><br/>Carts: <strong>{result.tiktok.sourcePerformance.addToCarts}</strong><br/>Checkout: <strong>{result.tiktok.sourcePerformance.checkoutStarts}</strong><br/>Orders: <strong>{result.tiktok.sourcePerformance.purchases}</strong><br/>Conversion: <strong>{pct(result.tiktok.sourcePerformance.conversion)}</strong><br/>Revenue: <strong>{money(result.tiktok.sourcePerformance.revenue)}</strong></p>:<p>No TikTok-specific SKU telemetry has been received yet.</p>}</article></div></section><section className="panel" style={{display:"block"}}><p className="eyebrow">ROOT-CAUSE ANALYSIS</p><h2>{result.findings.length?`${result.findings.length} issues detected`:"No immediate issue detected"}</h2><div style={{display:"grid",gap:10}}>{result.findings.map((f:any,i:number)=><article className="module" key={`${f.layer}-${i}`}><div style={{display:"flex",gap:9,alignItems:"center"}}><span style={{width:16,height:16,borderRadius:"50%",background:color(f.severity==="CRITICAL"?"RED":"YELLOW")}}/><p className="eyebrow" style={{margin:0}}>{f.layer} · {f.severity}</p></div><h3>{f.title}</h3><p><strong>Observation:</strong> {f.observation}</p><p><strong>Recommended:</strong> {f.recommendation}</p><p><strong>Confidence:</strong> {Math.round(f.confidence*100)}%</p><AutoFixButton channelId="tiktok" channelName="TikTok" layer={f.layer} title={f.title} observation={f.observation} likelyCause={f.likelyCause??f.observation} recommendation={f.recommendation} confidence={f.confidence}/></article>)}</div></section></>}</main>}
