const baseUrl=(process.env.WOOCOMMERCE_URL??process.env.WC_STORE_URL??"https://laughingmoosegifts.com").replace(/\/$/,"");

function credentials(){const key=process.env.WOOCOMMERCE_CONSUMER_KEY??process.env.WC_CONSUMER_KEY;const secret=process.env.WOOCOMMERCE_CONSUMER_SECRET??process.env.WC_CONSUMER_SECRET;if(!key||!secret)throw new Error("WooCommerce API credentials are not configured.");return{key,secret};}
export function woocommerceConfigured(){return Boolean((process.env.WOOCOMMERCE_CONSUMER_KEY??process.env.WC_CONSUMER_KEY)&&(process.env.WOOCOMMERCE_CONSUMER_SECRET??process.env.WC_CONSUMER_SECRET));}
export async function wooRequest<T>(path:string,params:Record<string,string|number|boolean|undefined>={}):Promise<T>{const{key,secret}=credentials();const url=new URL(`${baseUrl}/wp-json/wc/v3${path}`);for(const[k,v]of Object.entries(params))if(v!==undefined)url.searchParams.set(k,String(v));url.searchParams.set("consumer_key",key);url.searchParams.set("consumer_secret",secret);const r=await fetch(url,{headers:{Accept:"application/json"},cache:"no-store"});if(!r.ok){const body=await r.text();throw new Error(`WooCommerce API request failed (${r.status}): ${body.slice(0,400)}`);}return r.json() as Promise<T>;}
